[project]
name = "shared-storage"
version = "0.1.0"
dependencies = [
    "boto3",
    "boto3-stubs",
    "botocore",
    "pydantic-settings"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"