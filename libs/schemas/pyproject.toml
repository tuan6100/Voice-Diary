[project]
name = "shared-schemas"
version = "0.1.0"
description = "Shared Pydantic models and Event definitions"
dependencies = [
    "pydantic>=2.5.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/shared_schemas"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"